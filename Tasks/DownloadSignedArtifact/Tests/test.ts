import { Task } from '../task';
import axios from 'axios';
import sinon from 'sinon';

const sandbox = sinon.createSandbox();

it('cannot detect corresponding submit signing request in the pipeline timeline', async () => {
    console.log('Test');
});
